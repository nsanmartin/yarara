#include <Media.hpp>
#include <Polygon/Polygon.hpp>


void Polygon::Draw(Media& media) const {
    //todo:
    // std::vector<Point> vertices{mVertices};
    // std::sort(vertices.begin(), vertices.end(), []
    //           (const Point& p, const Point& q) {
    //               return p.mX < q.mX;
    //           });


    // int from{0};
    // int count{1};
    // int used{};
    // int y0{vertices[0].mY};
    // std::vector<Point> up;
    // std::vector<Point> down;
    // assert (vertices.size() >= 3);
    // for (auto& p : vertices) {
    //     if (p.mY >= y0) { up.push_back(p); }
    //     else { down.push_back(p); }
    // }
    // std::vector<int> triangle;
    // for (auto& p : up) {
    //     triangle.push_back(p);
    //     if (triangle.size() == 3) {  /* draw triangle */ }
    // }

    // for (auto& p : down) {
    //     triangle.push_back(p);
    //     if (triangle.size() == 3) {  /* draw triangle */ }
    // }

    //...
}

